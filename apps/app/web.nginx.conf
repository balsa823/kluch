server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Expo Router static export: try exact file, then .html, then SPA fallback.
  location / {
    try_files $uri $uri.html $uri/ /index.html;
  }
}
