terraform {
  required_version = ">= 1.5"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
    random = {
      source  = "hashicorp/random"
      version = "~> 3.0"
    }
  }
}

provider "azurerm" {
  features {}

  # Auth comes from the az CLI (machine is already `az login`'d). No hardcoded creds.
  subscription_id = var.subscription_id
}

provider "random" {}
