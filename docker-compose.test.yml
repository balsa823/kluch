services:
  postgres-test:
    image: postgres:16
    environment:
      POSTGRES_USER: kluch
      POSTGRES_PASSWORD: kluch
      POSTGRES_DB: kluch_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
